version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: omeropsmap_postgres
    environment:
      POSTGRES_USER: omeropsmap
      POSTGRES_PASSWORD: omeropsmap_dev_pass
      POSTGRES_DB: omeropsmap
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U omeropsmap"]
      interval: 10s
      timeout: 5s
      retries: 5

  data_server:
    build: .
    container_name: omeropsmap_data_server
    ports:
      - "8001:8001"
    environment:
      - DATABASE_URL=postgresql+asyncpg://omeropsmap:omeropsmap_dev_pass@postgres:5432/omeropsmap
      - SECRET_KEY=${SECRET_KEY:-dev-secret-key-change-in-production}
      - ALGORITHM=HS256
      - JWT_EXPIRE_HOURS=24
      # Initial Admin (optional - will auto-create on first startup)
      - INITIAL_ADMIN_USERNAME=${INITIAL_ADMIN_USERNAME:-}
      - INITIAL_ADMIN_PASSWORD=${INITIAL_ADMIN_PASSWORD:-}
      - INITIAL_ADMIN_DISPLAY_NAME=${INITIAL_ADMIN_DISPLAY_NAME:-מנהל ראשי}
      - INITIAL_ADMIN_EMAIL=${INITIAL_ADMIN_EMAIL:-}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./app:/app/app
      - ./scripts:/app/scripts
      - ./start.sh:/app/start.sh
    command: bash /app/start.sh

volumes:
  postgres_data:

